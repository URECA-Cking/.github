# 협업 규칙

## 브랜치

| 브랜치 | 의미 | 배포 |
| --- | --- | --- |
| `main` | 릴리스된 상태 | 운영 서버 (예정) |
| `develop` | 개발 통합 | 머지 시 개발 서버 자동 배포 |
| `<타입>/<이슈번호>-<요약>` | 작업 브랜치 | 없음 |

- 접두어는 아래 커밋 타입과 같은 걸 쓴다. 예) `feat/12-apply-ticket`, `chore/5-setup-ci`
- 이슈 라벨(`feature`/`bug`/`task`)과 브랜치 접두어가 꼭 같을 필요는 없다.
  `task` 이슈여도 문서 작업이면 `docs/`, 설정 작업이면 `chore/`를 쓴다.
- `main`, `develop`은 직접 push 금지. PR로만 반영한다.
- 평소 PR은 `develop`으로 보낸다. `develop → main`은 릴리스할 때만 올리고, 머지 후 버전 태그를 단다.

## 작업 흐름

1. 이슈를 만들고 **자신을 담당자로 지정한다.** (작업 단위 = 이슈 하나)
2. `develop`에서 작업 브랜치를 딴다.
3. PR을 `develop`으로 올리고 본문에 `Closes #12`를 적는다.
4. 리뷰어 1명 이상 approve 후 **Squash and merge**, 브랜치는 삭제한다.

## 커밋 메시지

`<타입>: <한국어 한 줄 요약>` 예) `feat: 응모권 적립 API 추가`

| 타입 | 용도 |
| --- | --- |
| feat | 기능 추가 |
| fix | 버그 수정 |
| refactor | 동작 변화 없는 구조 개선 |
| docs | 문서 |
| test | 테스트 |
| ci | CI/CD 워크플로우, 배포 스크립트 |
| chore | 빌드·설정·의존성 등 나머지 |

- 한 커밋은 하나의 관심사만 담는다.
- 본문에는 "무엇을"보다 **왜 그렇게 했는지**를 남긴다.

> 표에 없는 타입이 필요하면 팀에 말하고 표에 추가한다. 즉흥적으로 새 타입을 만들지는 않는다.

## 리뷰

- PR을 올릴 때 **리뷰어를 직접 지정한다.** (Reviewers에서 선택)
- 24시간 안에 리뷰한다. 늦어지면 팀 채널에 알린다.
- 수정 요청은 이유를 적는다. 취향 차이는 `nit:`을 붙여 참고 의견임을 표시한다.

## 하지 말 것

- `.env`, API 키, 토큰 등 시크릿 커밋
- `main`, `develop` 직접 push, force push
